---
// src/components/Header.astro
import SocialIcons from './SocialIcons.astro';
const nav = [
  { href: '/', label: 'Inicio' },
  { href: '/categorias', label: 'Categorías' },
  { href: '/sobre', label: 'Sobre el proyecto' },
  { href: '/contacto', label: 'Contacto' },
];
---
<header class="border-b border-slate-200 dark:border-slate-800">
  <div class="mx-auto flex max-w-4xl items-center justify-between px-4 py-5">
    <a href="/" class="flex flex-col leading-tight">
      <span class="font-serif text-2xl font-bold tracking-tight text-slate-900 dark:text-white">Análisis</span>
      <span class="text-xs text-slate-500 dark:text-slate-400">Geopolítica y análisis político</span>
    </a>
    <nav class="flex items-center gap-6">
      {nav.map((n) => (
        <a href={n.href} class="hidden text-sm text-slate-600 transition hover:text-accent-600 dark:text-slate-300 dark:hover:text-accent-400 sm:block">
          {n.label}
        </a>
      ))}
      <SocialIcons />
    </nav>
  </div>
</header>
